# bin is created when building luet-mtree
bin/
# coverage.txt is creaated when running the tests
coverage.txt
# build is generated when creating repos for the luet package
build/
# dist is generated byt goreleaser when building binaries
dist/
